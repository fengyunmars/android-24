<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:orientation="vertical" android:background="@color/layout_background" android:layout_width="fill_parent" android:layout_height="fill_parent"
  xmlns:android="http://schemas.android.com/apk/res/android">
    <include android:id="@id/title_layout" layout="@layout/common_title_view" />
    <ScrollView android:layout_below="@id/title_layout" style="@style/matchxy">
        <LinearLayout style="@style/matchxy">
            <ListView android:id="@id/cpu_list" android:layout_margin="10.0dip" android:divider="@null" style="@style/matchxy" />
        </LinearLayout>
    </ScrollView>
</RelativeLayout>